---
import { Image } from "astro:assets";

export interface Props {
  src: ImageMetadata;
  alt?: string;
}

const { src, alt = "" } = Astro.props;
---

<Image {...{ src, alt }} format="avif" class="rounded-lg object-cover" />
