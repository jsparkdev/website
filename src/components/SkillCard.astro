---
import { Icon } from 'astro-icon/components'

interface Props {
	icon: string
	name: string
}

const { icon, name } = Astro.props
const size = 20
---

<div class="flex items-center gap-1 bg-slate-600 py-1 px-3 rounded-xl">
  <Icon name={icon} size={size} />
  {name}
</div>