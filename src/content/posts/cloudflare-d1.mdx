---
title: Cloudflare D1
description: 간단한 예시를 통해 Cloudflare D1 데이터베이스를 사용하는 방법에 대해 알아봅니다.
---

다음 명령어를 사용하여 데이터베이스를 생성합니다. 아래 예시 코드는 `workers-d1`이라는 이름의 데이터베이스를 생성합니다.

```bash
pnpm wrangler d1 create workers-d1
```

데이터베이스 생성에 성공했다면 다음과 같은 형태의 메시지가 콘솔에 출력됩니다. 이는 workers에 제공될 데이터베이스 정보입니다. 이 데이터를 `wrangler.toml` 파일에 추가합니다.

```toml
[[d1_databases]]
binding = "DB" # available in your Worker on env.DB
database_name = "workers-d1"
database_id = "<unique-ID-for-your-database>"
```

성공적으로 데이터베이스를 생성했으므로 이제 데이터베이스를 사용할 수 있습니다.

우선 데이터베이스에 테이블을 생성한 후, 데이터를 삽입하겠습니다. 이 작업을 명령줄에서 실행할 수도 있지만, 별도의 스키마 파일을 생성하여 사용하는 것이 더 편리합니다.

`schema.sql` 파일을 생성한 후, 다음 코드를 추가합니다. 이 파일은 `Customers` 테이블을 생성하고, 데이터를 삽입합니다.

```sql
DROP TABLE IF EXISTS Customers;
CREATE TABLE IF NOT EXISTS Customers (CustomerId INTEGER PRIMARY KEY, CompanyName TEXT, ContactName TEXT);
INSERT INTO Customers (CustomerID, CompanyName, ContactName) VALUES (1, 'Alfreds Futterkiste', 'Maria Anders'), (4, 'Around the Horn', 'Thomas Hardy'), (11, 'Bs Beverages', 'Victoria Ashworth'), (13, 'Bs Beverages', 'Random Name');
```

`wrangler`는 데이터베이스에 쿼리를 실행할 수 있는 명령을 제공합니다. 우리가 생성한 데이터베이스에 `schema.sql` 파일을 실행하기 위해 다음 명령을 실행합니다.

```bash
pnpm wrangler d1 execute workers-d1 --local --file=./schema.sql
```

이 명령은 로컬 데이터베이스에 `schema.sql` 파일을 실행합니다.

로컬 데이터베이스가 제대로 초기화되었는지 확인하기 위해 다음 명령을 실행합니다. 이 명령은 `Customers` 테이블의 모든 데이터를 반환합니다.

```bash
pnpm wrangler d1 execute workers-d1 --local --command="SELECT * FROM Customers"
```

이렇게 로컬 데이터베이스를 초기화하였습니다. 이제 Workers에서 데이터베이스를 사용할 수 있는 코드를 작성해야 합니다.

Workers에서는 D1 클라이언트를 사용하여 데이터베이스와 상호작용할 수 있습니다.

우선 다음 명령을 통해 D1 클라이언트의 타입을 정의합니다.
  
```bash
pnpm wrangler types
```

타입을 정의했다면, 아래와 같이 fetch 핸들러의 `env` 매개변수를 통해 데이터베이스에 접근할 수 있습니다.

```ts
export default {
	async fetch(request: Request, env: Env) {
		const { pathname } = new URL(request.url);

		if (pathname === "/api/beverages") {
			const { results } = await env.DB.prepare(
				"SELECT * FROM Customers WHERE CompanyName = ?",
			)
				.bind("Bs Beverages")
				.all();

			return Response.json(results);
		}

		return new Response(
			"Call /api/beverages to see everyone who works at Bs Beverages",
		);
	},
};
```

로컬 개발 모드에서 이 작업을 수행했다면, 원격 데이터베이스에도 이 작업을 똑같이 적용한 후, Workers에 배포할 수 있습니다.

```bash
pnpm wrangler d1 execute workers-d1 --remote --file=./schema.sql
pnpm wrangler d1 execute workers-d1 --remote --command="SELECT * FROM Customers"
pnpm wrangler deploy
```

## 참고 자료

- https://developers.cloudflare.com/d1